---
phase: 01-validate-multi-agent-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tradingagents/graph/state_validation.py
  - tradingagents/graph/trading_graph.py
  - tradingagents/graph/egyptian_trading_graph.py
  - tradingagents/default_config.py
  - tradingagents/egyptian_config.py
autonomous: true
must_haves:
  truths:
    - "Validation runs before state logging and blocks propagation when required fields are missing or invalid."
    - "US and EGX graph propagation can toggle validation via config flags."
    - "Validation failures surface actionable diagnostics identifying missing or invalid state data."
  artifacts:
    - path: "tradingagents/graph/state_validation.py"
      provides: "State validation helpers for multi-agent runs"
      exports: ["validate_agent_state", "format_validation_errors"]
    - path: "tradingagents/graph/trading_graph.py"
      provides: "US graph propagate validation gate"
      contains: "validate_agent_state"
    - path: "tradingagents/graph/egyptian_trading_graph.py"
      provides: "EGX graph propagate validation gate"
      contains: "validate_agent_state"
    - path: "tradingagents/default_config.py"
      provides: "Default config flag for validation"
      contains: "validate_state"
    - path: "tradingagents/egyptian_config.py"
      provides: "Egyptian config flag for validation"
      contains: "validate_state"
  key_links:
    - from: "tradingagents/graph/trading_graph.py"
      to: "tradingagents/graph/state_validation.py"
      via: "validate_agent_state call in propagate"
      pattern: "validate_agent_state"
    - from: "tradingagents/graph/egyptian_trading_graph.py"
      to: "tradingagents/graph/state_validation.py"
      via: "validate_agent_state call in propagate"
      pattern: "validate_agent_state"
---

<objective>
Introduce state validation guardrails for US and EGX multi-agent graphs so missing outputs are detected early and decisions are validated before logging.

Purpose: Ensure multi-agent core outputs are reliable and failures are diagnosed with clear, actionable errors.
Output: Shared validation utilities, config flags, and graph integration that enforce required state fields.
</objective>

<execution_context>
@/Users/ahmedmohamed/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/ahmedmohamed/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@tradingagents/agents/utils/agent_states.py
@tradingagents/graph/propagation.py
@tradingagents/graph/trading_graph.py
@tradingagents/graph/egyptian_trading_graph.py
@tradingagents/default_config.py
@tradingagents/egyptian_config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared state validation utilities</name>
  <files>tradingagents/graph/state_validation.py</files>
  <action>
Create a new validation module that checks final graph state completeness and decision validity.

- Define required field sets for US and EGX runs (including debate state keys and report fields).
- Allow EGX to satisfy report requirements via either egyptian_* fields or mapped standard fields.
- Validate that final_trade_decision normalizes to BUY/SELL/HOLD; include normalized value in diagnostics.
- Return structured errors (missing keys, empty values, invalid decision) and provide a formatter helper for readable messages.
- Keep validation pure (no side effects) so it can be reused by a runner script.
  </action>
  <verify>python -m py_compile tradingagents/graph/state_validation.py</verify>
  <done>Validation module exists with explicit required keys for US/EGX and returns structured diagnostics for missing/invalid fields.</done>
</task>

<task type="auto">
  <name>Task 2: Wire validation into US/EGX graph propagation</name>
  <files>tradingagents/graph/trading_graph.py, tradingagents/graph/egyptian_trading_graph.py, tradingagents/default_config.py, tradingagents/egyptian_config.py</files>
  <action>
Integrate the validation module into graph execution and expose a config flag to toggle it.

- Add a boolean config flag (validate_state) to DEFAULT_CONFIG and EGYPTIAN_CONFIG with a safe default of True.
- In TradingAgentsGraph.propagate and EgyptianTradingAgentsGraph.propagate, call validate_agent_state(final_state, market=...) before _log_state.
- If validation errors exist, raise a ValueError with the formatted diagnostics so failures are explicit and actionable.
- Keep validation opt-out by respecting config["validate_state"] (skip if False).
  </action>
  <verify>python -m py_compile tradingagents/graph/trading_graph.py tradingagents/graph/egyptian_trading_graph.py tradingagents/default_config.py tradingagents/egyptian_config.py</verify>
  <done>Graph propagate paths enforce validation when enabled and fail fast with readable diagnostics.</done>
</task>

</tasks>

<verification>
- Validation runs without syntax errors and produces diagnostics before logging when required fields are missing.
</verification>

<success_criteria>
- Validation is integrated into both US and EGX propagate flows and can be toggled via config.
- Missing or empty required fields produce a clear ValueError before state logging occurs.
</success_criteria>

<output>
After completion, create `.planning/phases/01-validate-multi-agent-core/01-01-SUMMARY.md`
</output>
