---
phase: 01-validate-multi-agent-core
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - tradingagents/agents/managers/risk_manager.py
  - tradingagents/graph/state_validation.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Final trade decision stored in state is a normalized BUY/SELL/HOLD value."
    - "Risk judge rationale remains available while the decision is a single normalized value."
  artifacts:
    - path: "tradingagents/agents/managers/risk_manager.py"
      provides: "Risk manager emits normalized final_trade_decision and preserves rationale."
    - path: "tradingagents/graph/state_validation.py"
      provides: "Decision normalization recognizes explicit decision markers."
  key_links:
    - from: "tradingagents/agents/managers/risk_manager.py"
      to: "tradingagents/graph/state_validation.py"
      via: "normalize_trade_decision import"
      pattern: "normalize_trade_decision"
    - from: "tradingagents/agents/managers/risk_manager.py"
      to: "final_trade_decision"
      via: "normalized decision assignment"
      pattern: "final_trade_decision"
---

<objective>
Normalize final trade decisions so US/EGX runs store BUY/SELL/HOLD values and retain risk-judge rationale.

Purpose: Close runtime gaps where final_trade_decision returns narratives instead of normalized decisions.
Output: Risk manager emits normalized decisions; decision normalization handles explicit decision markers.
</objective>

<execution_context>
@/Users/ahmedmohamed/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/ahmedmohamed/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-validate-multi-agent-core/01-validate-multi-agent-core-VERIFICATION.md
@tradingagents/agents/managers/risk_manager.py
@tradingagents/graph/state_validation.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize risk manager output and fix fundamentals input</name>
  <files>tradingagents/agents/managers/risk_manager.py</files>
  <action>
    Update the risk manager to output a normalized final decision while keeping a full rationale:
    - Fix the fundamentals report assignment to use state["fundamentals_report"] (not news_report).
    - Update the prompt to require a structured output with a first line like "Decision: BUY/SELL/HOLD" followed by a "Rationale:" section.
    - Parse the response to extract the decision line using a regex; fall back to normalize_trade_decision on the full content if needed.
    - If no normalized decision can be derived, raise ValueError so validation fails loudly.
    - Set final_trade_decision to the normalized decision, and keep risk_debate_state.judge_decision as the full response content.
  </action>
  <verify>python -m py_compile tradingagents/agents/managers/risk_manager.py</verify>
  <done>final_trade_decision is a single BUY/SELL/HOLD value, and judge_decision retains the narrative rationale.</done>
</task>

<task type="auto">
  <name>Task 2: Improve decision normalization for explicit markers</name>
  <files>tradingagents/graph/state_validation.py</files>
  <action>
    Enhance normalize_trade_decision to recognize explicit decision markers before token scanning:
    - Detect patterns like "Decision: BUY" or "FINAL TRANSACTION PROPOSAL: **SELL**" (case-insensitive, word-boundary safe).
    - Return the matched decision if found.
    - Preserve existing fallback behavior for empty inputs and general token detection.
  </action>
  <verify>python -m py_compile tradingagents/graph/state_validation.py</verify>
  <done>normalize_trade_decision returns BUY/SELL/HOLD when an explicit decision marker is present.</done>
</task>

</tasks>

<verification>
- Risk manager emits normalized final_trade_decision and raises if unparseable.
- Decision normalization recognizes explicit decision markers in narratives.
</verification>

<success_criteria>
- US/EGX runs store BUY/SELL/HOLD in final_trade_decision and retain judge rationale in risk_debate_state.
</success_criteria>

<output>
After completion, create `.planning/phases/01-validate-multi-agent-core/01-03-SUMMARY.md`
</output>
