---
phase: 01-validate-multi-agent-core
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - scripts/validate_multi_agent_core.py
  - README.md
autonomous: true
must_haves:
  truths:
    - "A validation runner can execute US and EGX propagations and summarize pass/fail outcomes."
    - "Runner output includes validation diagnostics when a run fails."
    - "Runner writes a JSON summary report for requested runs."
    - "README documents how to invoke the validation runner with examples."
  artifacts:
    - path: "tradingagents/graph/state_validation.py"
      provides: "State validation helpers for multi-agent runs"
      exports: ["validate_agent_state", "format_validation_errors"]
    - path: "scripts/validate_multi_agent_core.py"
      provides: "CLI validation runner for US + EGX flows"
      contains: "argparse"
    - path: "README.md"
      provides: "Validation usage documentation"
      contains: "validate_multi_agent_core.py"
  key_links:
    - from: "scripts/validate_multi_agent_core.py"
      to: "TradingAgentsGraph/EgyptianTradingAgentsGraph"
      via: "propagate"
      pattern: "propagate"
    - from: "scripts/validate_multi_agent_core.py"
      to: "tradingagents/graph/state_validation.py"
      via: "validate_agent_state"
      pattern: "validate_agent_state"
---

<objective>
Add a repeatable validation runner and documentation so the multi-agent core can be smoke-tested for US and EGX flows.

Purpose: Provide an executable, repeatable way to verify multi-agent core stability with clear pass/fail output.
Output: A CLI script for validation and README documentation on how to run it.
</objective>

<execution_context>
@/Users/ahmedmohamed/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/ahmedmohamed/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-validate-multi-agent-core/01-01-SUMMARY.md
@tradingagents/graph/trading_graph.py
@tradingagents/graph/egyptian_trading_graph.py
@tradingagents/graph/state_validation.py
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a multi-agent core validation runner</name>
  <files>scripts/validate_multi_agent_core.py</files>
  <action>
Create a CLI script that runs US and EGX graph propagations and reports validation outcomes.

- Use argparse to accept flags for markets (us/egx), symbols, trade dates, and an output path for a JSON summary.
- Instantiate TradingAgentsGraph and EgyptianTradingAgentsGraph with config overrides (respect existing defaults).
- Run propagate for each requested market/symbol/date and capture success/failure along with validation diagnostics.
- After each propagate, explicitly call validate_agent_state on the final state and include format_validation_errors output in failure diagnostics.
- Emit a concise console summary and write a JSON report (create output directory if missing).
- Exit non-zero if any run fails validation or raises an exception.
  </action>
  <verify>python scripts/validate_multi_agent_core.py --help</verify>
  <done>Validation runner executes with --help and provides structured pass/fail output for US and EGX runs.</done>
</task>

<task type="auto">
  <name>Task 2: Document validation runner usage</name>
  <files>README.md</files>
  <action>
Add a README section describing how to run the validation script, including example commands for US-only, EGX-only, and combined runs, plus where the JSON summary is written.
  </action>
  <verify>grep -n "validate_multi_agent_core" -n README.md</verify>
  <done>README documents the validation script and provides runnable example commands.</done>
</task>

</tasks>

<verification>
- Validation script can be invoked with --help and produces a JSON summary when run.
</verification>

<success_criteria>
- A repeatable script exists to smoke-test US and EGX multi-agent flows and report pass/fail outcomes.
- Documentation clearly instructs how to run the validation script.
</success_criteria>

<output>
After completion, create `.planning/phases/01-validate-multi-agent-core/01-02-SUMMARY.md`
</output>
