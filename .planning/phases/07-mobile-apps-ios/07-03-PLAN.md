---
phase: 07-mobile-apps-ios
plan: 03
type: execute
wave: 3
depends_on: [07-01, 07-02]
files_modified:
  - /Users/ahmedmohamed/Desktop/trading/TradingAgents-iOS/Features/TickerDetail/TickerDetailView.swift
  - /Users/ahmedmohamed/Desktop/trading/TradingAgents-iOS/Features/TickerDetail/TickerDetailViewModel.swift
  - /Users/ahmedmohamed/Desktop/trading/TradingAgents-iOS/Services/AnalysisPollingService.swift
  - /Users/ahmedmohamed/Desktop/trading/TradingAgents-iOS/SharedUI/ErrorBanner.swift
  - /Users/ahmedmohamed/Desktop/trading/TradingAgents-iOS/SharedUI/LoadingView.swift
autonomous: true
must_haves:
  truths:
    - "User can view price chart and latest snapshot data for a selected ticker."
    - "User can request analysis and see status updates and results in-app."
    - "Rate-limit and API errors are shown with clear retry guidance."
  artifacts:
    - path: "/Users/ahmedmohamed/Desktop/trading/TradingAgents-iOS/Features/TickerDetail/TickerDetailView.swift"
      provides: "Ticker detail UI with chart, snapshot, and analysis results"
    - path: "/Users/ahmedmohamed/Desktop/trading/TradingAgents-iOS/Services/AnalysisPollingService.swift"
      provides: "Polling loop for analytics report status"
  key_links:
    - from: "/Users/ahmedmohamed/Desktop/trading/TradingAgents-iOS/Features/TickerDetail/TickerDetailViewModel.swift"
      to: "/api/v1/historical/daily"
      via: "APIClient.getDailyHistory"
      pattern: "getDailyHistory"
    - from: "/Users/ahmedmohamed/Desktop/trading/TradingAgents-iOS/Features/TickerDetail/TickerDetailViewModel.swift"
      to: "/api/v1/analytics/report"
      via: "APIClient.createReport"
      pattern: "createReport"
---

<objective>
Deliver the ticker detail experience with charts, snapshot data, and analysis request/polling so users can view insights on mobile.

Purpose: Complete the mobile POC by connecting historical data, snapshots, and async analytics results in a single screen.
Output: Ticker detail screen with Swift Charts, analysis polling, and robust error handling.
</objective>

<execution_context>
@/Users/ahmedmohamed/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/ahmedmohamed/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-mobile-apps-ios/07-CONTEXT.md
@.planning/phases/07-mobile-apps-ios/07-RESEARCH.md
@tradingagents/api/routers/snapshots.py
@tradingagents/api/routers/historical.py
@tradingagents/api/routers/analytics.py
@tradingagents/api/schemas/analytics.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ticker detail view with chart + snapshot</name>
  <files>
    /Users/ahmedmohamed/Desktop/trading/TradingAgents-iOS/Features/TickerDetail/TickerDetailView.swift
    /Users/ahmedmohamed/Desktop/trading/TradingAgents-iOS/Features/TickerDetail/TickerDetailViewModel.swift
  </files>
  <action>
    Implement `TickerDetailViewModel` to load snapshot (GET /snapshots) and daily history (GET /historical/daily) for the selected market/ticker.
    Add range controls (1W, 1M, 3M, 1Y) that compute start_date/end_date for daily history; cap bars if needed for smooth charts.
    Build `TickerDetailView` with Swift Charts for price (line) and volume (bar), and a summary header showing last price, change %, volume, session status, and freshness label.
  </action>
  <verify>
    xcodebuild -project "/Users/ahmedmohamed/Desktop/trading/TradingAgents-iOS/TradingAgentsMobile.xcodeproj" -scheme "TradingAgentsMobile" -destination "generic/platform=iOS" build
  </verify>
  <done>
    Ticker detail screen renders a chart and snapshot data for the selected symbol with working range controls.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement analysis request + polling results</name>
  <files>
    /Users/ahmedmohamed/Desktop/trading/TradingAgents-iOS/Services/AnalysisPollingService.swift
    /Users/ahmedmohamed/Desktop/trading/TradingAgents-iOS/Features/TickerDetail/TickerDetailViewModel.swift
    /Users/ahmedmohamed/Desktop/trading/TradingAgents-iOS/Features/TickerDetail/TickerDetailView.swift
  </files>
  <action>
    Create `AnalysisPollingService` that starts a Task loop polling `GET /analytics/report/{id}` every 5s and supports cancellation on view disappear.
    In `TickerDetailViewModel`, add an action to submit `POST /analytics/report` with market_id, symbol, analysis_date (today), lookback_days (e.g., 90), online=true, and default indicators; store report_id and status.
    Render analysis status (queued/running/completed/error) and, when completed, show decision label + confidence, summary text, support/resistance list, and risk notes.
  </action>
  <verify>
    xcodebuild -project "/Users/ahmedmohamed/Desktop/trading/TradingAgents-iOS/TradingAgentsMobile.xcodeproj" -scheme "TradingAgentsMobile" -destination "generic/platform=iOS" build
  </verify>
  <done>
    Users can trigger analysis, see status updates, and view final results in the ticker detail screen.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add error + rate-limit handling for detail screen</name>
  <files>
    /Users/ahmedmohamed/Desktop/trading/TradingAgents-iOS/Features/TickerDetail/TickerDetailView.swift
    /Users/ahmedmohamed/Desktop/trading/TradingAgents-iOS/Features/TickerDetail/TickerDetailViewModel.swift
    /Users/ahmedmohamed/Desktop/trading/TradingAgents-iOS/SharedUI/ErrorBanner.swift
    /Users/ahmedmohamed/Desktop/trading/TradingAgents-iOS/SharedUI/LoadingView.swift
  </files>
  <action>
    Map `APIClientError.rateLimited` to a visible message that includes Retry-After seconds and disables the analysis button until the backoff expires.
    Show error banners for snapshot/history/analytics failures with a retry button that re-fetches data or restarts polling.
    Ensure view model updates are `@MainActor` to avoid SwiftUI thread warnings during polling.
  </action>
  <verify>
    xcodebuild -project "/Users/ahmedmohamed/Desktop/trading/TradingAgents-iOS/TradingAgentsMobile.xcodeproj" -scheme "TradingAgentsMobile" -destination "generic/platform=iOS" build
  </verify>
  <done>
    Ticker detail screen surfaces errors clearly and respects rate-limit backoff without crashing.
  </done>
</task>

</tasks>

<verification>
Run the app in the iOS simulator and confirm:
1) Chart renders with range toggles and snapshot header updates.
2) Analysis request shows queued/running and ends with decision + summary.
3) Rate-limit and API errors display a clear retry guidance message.
</verification>

<success_criteria>
- Ticker detail combines charts, snapshots, and analysis results in one screen.
- Analysis polling is cancellable and updates UI safely on the main actor.
- Error and rate-limit handling is consistent and user-friendly.
</success_criteria>

<output>
After completion, create `.planning/phases/07-mobile-apps-ios/07-03-SUMMARY.md`
</output>
