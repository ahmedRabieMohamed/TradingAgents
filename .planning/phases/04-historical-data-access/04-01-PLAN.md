---
phase: 04-historical-data-access
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tradingagents/api/schemas/historical.py
  - tradingagents/api/services/eodhd_client.py
  - tradingagents/api/settings.py
autonomous: true
user_setup:
  - service: eodhd
    why: "Historical prices and corporate actions"
    env_vars:
      - name: EODHD_API_KEY
        source: "EODHD Dashboard -> API token"

must_haves:
  truths:
    - "User can receive typed daily and intraday historical payloads with explicit adjusted-close fields."
    - "User can receive corporate actions payloads (dividends/splits) in a consistent schema."
    - "User can see freshness metadata (as_of, age, cache status) on historical payloads."
  artifacts:
    - path: "tradingagents/api/schemas/historical.py"
      provides: "Pydantic models for daily, intraday, and corporate actions responses"
    - path: "tradingagents/api/services/eodhd_client.py"
      provides: "EODHD methods for intraday, dividends, and splits"
      exports: ["get_intraday_series", "get_dividends", "get_splits"]
    - path: "tradingagents/api/settings.py"
      provides: "Historical/intraday cache TTL settings"
  key_links:
    - from: "tradingagents/api/services/eodhd_client.py"
      to: "https://eodhd.com/api/intraday"
      via: "get_intraday_series endpoint path"
      pattern: "intraday/\{symbol\}\.\{exchange\}"
    - from: "tradingagents/api/services/eodhd_client.py"
      to: "https://eodhd.com/api/div"
      via: "get_dividends endpoint path"
      pattern: "div/\{symbol\}\.\{exchange\}"
    - from: "tradingagents/api/services/eodhd_client.py"
      to: "https://eodhd.com/api/splits"
      via: "get_splits endpoint path"
      pattern: "splits/\{symbol\}\.\{exchange\}"
---

<objective>
Define historical API schemas and extend the EODHD client to support intraday and corporate actions.

Purpose: Establish typed payloads and provider access methods used by historical services and routers.
Output: New historical schemas, updated EODHD client methods, and cache TTL settings.
</objective>

<execution_context>
@/Users/ahmedmohamed/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/ahmedmohamed/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-historical-data-access/04-RESEARCH.md
@tradingagents/api/services/eodhd_client.py
@tradingagents/api/services/eodhd_cache.py
@tradingagents/api/schemas/snapshots.py
@tradingagents/api/settings.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add historical response schemas</name>
  <files>tradingagents/api/schemas/historical.py</files>
  <action>
Create Pydantic models for historical responses with explicit adjusted-close support and freshness metadata:
- HistoricalFreshness(as_of: datetime, age_seconds: int, cache_status: str)
- DailyBar(date: date, open/high/low/close: float, adjusted_close: Optional[float], volume: int)
- DailyHistoryResponse(market_id, symbol, currency, start_date, end_date, bars: list[DailyBar], adjusted_close_available: bool, freshness)
- IntradayCandle(timestamp: datetime, open/high/low/close: float, volume: int)
- IntradayHistoryResponse(market_id, symbol, currency, interval, range, timezone, start_timestamp, end_timestamp, candles: list[IntradayCandle], freshness)
- CorporateActionsResponse(market_id, symbol, dividends: list[DividendAction], splits: list[SplitAction], freshness)
- DividendAction(ex_date: date, amount: float, declaration_date/record_date/payment_date/currency as Optional)
- SplitAction(date: date, ratio: Optional[str], before: Optional[float], after: Optional[float])
  </action>
  <verify>Open historical schemas file and confirm all response models and fields exist with Pydantic BaseModel.</verify>
  <done>Historical schemas file defines daily, intraday, and corporate actions models with freshness metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Extend EODHD client and settings for historical data</name>
  <files>
tradingagents/api/services/eodhd_client.py
tradingagents/api/settings.py
  </files>
  <action>
Update settings to include cache TTL defaults for historical and intraday data (e.g., historical_cache_ttl_seconds, intraday_cache_ttl_seconds). Then extend EodhdClient with:
- get_intraday_series(symbol, exchange_code, start_ts, end_ts, interval) using endpoint "intraday/{symbol}.{exchange_code}" and cache key including interval + timestamps.
- get_dividends(symbol, exchange_code, start_date, end_date) using endpoint "div/{symbol}.{exchange_code}" with optional from/to params.
- get_splits(symbol, exchange_code, start_date, end_date) using endpoint "splits/{symbol}.{exchange_code}" with optional from/to params.
  </action>
  <verify>Read eodhd_client.py and confirm the three new methods exist with correct endpoint paths and cache keys.</verify>
  <done>Settings expose historical/intraday cache TTLs and the EODHD client supports intraday/dividends/splits fetches.</done>
</task>

</tasks>

<verification>
- Historical schemas compile with Pydantic BaseModel usage.
- EODHD client includes intraday/dividends/splits helpers with cache keying.
</verification>

<success_criteria>
- Historical response models exist for daily, intraday, and corporate actions with freshness metadata.
- EODHD client exposes intraday/dividends/splits methods and settings include TTLs for historical data.
</success_criteria>

<output>
After completion, create `.planning/phases/04-historical-data-access/04-01-SUMMARY.md`
</output>
