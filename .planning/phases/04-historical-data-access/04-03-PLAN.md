---
phase: 04-historical-data-access
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - tradingagents/api/routers/historical.py
  - tradingagents/api/routers/__init__.py
autonomous: true

must_haves:
  truths:
    - "User can request daily historical OHLCV via the versioned API."
    - "User can request intraday range history with entitlement checks."
    - "User can request corporate actions (dividends/splits) via the versioned API."
  artifacts:
    - path: "tradingagents/api/routers/historical.py"
      provides: "FastAPI router for historical endpoints"
      exports: ["router"]
    - path: "tradingagents/api/routers/__init__.py"
      provides: "Versioned API router includes historical endpoints"
  key_links:
    - from: "tradingagents/api/routers/historical.py"
      to: "tradingagents/api/services/historical.py"
      via: "service calls"
      pattern: "historical_service\.get_(daily_history|intraday_history|corporate_actions)"
    - from: "tradingagents/api/routers/__init__.py"
      to: "tradingagents/api/routers/historical.py"
      via: "include_router"
      pattern: "include_router\(historical_router\)"
---

<objective>
Expose historical data services via FastAPI routes and wire them into the versioned API router.

Purpose: Make historical prices and corporate actions accessible to API clients with standard auth/rate-limit enforcement.
Output: Historical router endpoints registered under /api/v1.
</objective>

<execution_context>
@/Users/ahmedmohamed/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/ahmedmohamed/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-historical-data-access/04-RESEARCH.md
@tradingagents/api/routers/snapshots.py
@tradingagents/api/routers/__init__.py
@tradingagents/api/deps/rate_limit.py
@tradingagents/api/deps/auth.py
@tradingagents/api/schemas/historical.py
@tradingagents/api/services/historical.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create historical data router endpoints</name>
  <files>tradingagents/api/routers/historical.py</files>
  <action>
Create a FastAPI router at /historical with three GET endpoints:
- /historical/daily: params market_id, symbol, start_date, end_date. Validate market (same pattern as snapshots), enforce rate limits, call historical_service.get_daily_history, return DailyHistoryResponse.
- /historical/intraday: params market_id, symbol, range (allowed: 1D/1W/1M/1Y). Add Depends(optional_current_account) to populate request.state, validate market, enforce rate limits, determine is_authenticated from request.state, call historical_service.get_intraday_history.
- /historical/actions: params market_id, symbol, start_date (optional), end_date (optional). Validate market, enforce rate limits, call historical_service.get_corporate_actions.
  </action>
  <verify>Open historical router file and confirm the three endpoints are defined with response_model mappings.</verify>
  <done>Historical daily, intraday, and corporate actions endpoints exist and enforce rate limits and market validation.</done>
</task>

<task type="auto">
  <name>Task 2: Wire historical router into API</name>
  <files>tradingagents/api/routers/__init__.py</files>
  <action>
Import the historical router and include it in api_router alongside snapshots, markets, symbols, and auth routers.
  </action>
  <verify>Check routers/__init__.py includes historical_router and api_router.include_router(historical_router).</verify>
  <done>Versioned API router exposes /historical endpoints.</done>
</task>

</tasks>

<verification>
- /historical/daily, /historical/intraday, and /historical/actions routes are registered under /api/v1.
</verification>

<success_criteria>
- Historical endpoints are accessible through the versioned router with rate-limit enforcement and optional auth context.
</success_criteria>

<output>
After completion, create `.planning/phases/04-historical-data-access/04-03-SUMMARY.md`
</output>
