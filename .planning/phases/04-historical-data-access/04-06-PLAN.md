---
phase: 04-historical-data-access
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - tradingagents/api/schemas/historical.py
  - tradingagents/api/services/historical.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Daily OHLCV responses never include bars outside the requested start_date/end_date range."
    - "When provider payloads include out-of-range bars, the response excludes them and surfaces range_filter diagnostics."
    - "Adjusted close availability reflects the filtered bar set."
  artifacts:
    - path: "tradingagents/api/schemas/historical.py"
      provides: "DailyRangeFilterDiagnostics and optional range_filter on DailyHistoryResponse"
    - path: "tradingagents/api/services/historical.py"
      provides: "Daily history range filtering and diagnostics logging"
  key_links:
    - from: "tradingagents/api/services/historical.py"
      to: "_filter_daily_bars_by_date"
      via: "get_daily_history post-normalization filtering"
      pattern: "get_daily_history.*_filter_daily_bars_by_date"
    - from: "tradingagents/api/services/historical.py"
      to: "DailyHistoryResponse.range_filter"
      via: "response construction"
      pattern: "range_filter="
---

<objective>
Enforce daily history date-range correctness and emit diagnostics when provider data falls outside the requested range.

Purpose: Prevent charts and analytics from ingesting out-of-range daily bars while keeping responses explainable.
Output: Range-filtered daily history responses with optional diagnostics metadata.
</objective>

<execution_context>
@/Users/ahmedmohamed/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/ahmedmohamed/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@tradingagents/api/services/historical.py
@tradingagents/api/schemas/historical.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add daily range filter diagnostics schema</name>
  <files>tradingagents/api/schemas/historical.py</files>
  <action>
    Add a DailyRangeFilterDiagnostics Pydantic model with fields:
    - dropped_count: int
    - dropped_min_date: Optional[date]
    - dropped_max_date: Optional[date]

    Extend DailyHistoryResponse with an optional field:
    - range_filter: Optional[DailyRangeFilterDiagnostics] = None

    Keep all existing fields unchanged to preserve backward compatibility.
  </action>
  <verify>python -m pytest -k historical</verify>
  <done>DailyHistoryResponse includes an optional range_filter field with diagnostics when provided.</done>
</task>

<task type="auto">
  <name>Task 2: Filter daily bars to requested date range and emit diagnostics</name>
  <files>tradingagents/api/services/historical.py</files>
  <action>
    Implement a helper (e.g., _filter_daily_bars_by_date) that:
    - Accepts normalized DailyBar list + start_date/end_date.
    - Filters inclusively so returned bars satisfy start_date <= bar.date <= end_date.
    - Computes diagnostics (dropped_count, dropped_min_date, dropped_max_date) based on removed bars.

    In get_daily_history:
    - Call the filter helper after _normalize_daily_bars.
    - Log a warning via logging.getLogger(__name__).warning when dropped_count > 0, including symbol, exchange_code, requested range, and dropped_count.
    - Set adjusted_close_available based on the filtered bars, not the pre-filter list.
    - Populate DailyHistoryResponse.range_filter only when dropped_count > 0 (otherwise None).

    Do not change cache behavior; filter at response-time so cached payloads remain raw while responses are correct.
  </action>
  <verify>python -m pytest -k historical</verify>
  <done>Daily history responses never include out-of-range bars and include diagnostics only when filtering occurs.</done>
</task>

</tasks>

<verification>
- python -m pytest -k historical
- (Optional runtime) curl "/api/v1/historical/daily?market_id=US&symbol=AAPL&start_date=2024-01-01&end_date=2024-02-01" returns bars whose dates are all within the requested range; if any are filtered, range_filter is present.
</verification>

<success_criteria>
- Daily history output excludes all bars outside [start_date, end_date].
- When filtering happens, response includes range_filter diagnostics and a warning is logged.
</success_criteria>

<output>
After completion, create `.planning/phases/04-historical-data-access/04-06-SUMMARY.md`
</output>
