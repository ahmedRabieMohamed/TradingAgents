---
phase: 03-snapshot-quotes-freshness
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - tradingagents/api/services/eodhd_client.py
  - tradingagents/api/services/snapshots.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Snapshot freshness labels reflect actual fetched_at from cache or provider data."
    - "Provider fetch path can bypass EODHD cache when cache_ttl_seconds=0."
  artifacts:
    - path: "tradingagents/api/services/eodhd_client.py"
      provides: "Cache TTL handling that preserves 0 for cache bypass"
      contains: "cache_ttl_seconds is None"
    - path: "tradingagents/api/services/snapshots.py"
      provides: "Freshness labeling based on real fetched_at timestamps"
      contains: "fetched_at"
  key_links:
    - from: "tradingagents/api/services/snapshots.py"
      to: "tradingagents/api/services/eodhd_client.py"
      via: "EodhdClient(cache_ttl_seconds=0)"
      pattern: "cache_ttl_seconds=0"
    - from: "tradingagents/api/services/snapshots.py"
      to: "tradingagents/api/services/eodhd_cache.py"
      via: "load_cached_payload_with_meta"
      pattern: "load_cached_payload_with_meta"
---

<objective>
Fix snapshot freshness labeling so cache bypass works and timestamps are preserved.

Purpose: Close verification gaps blocking API-04 freshness accuracy.
Output: Snapshot service uses real fetched_at timestamps and a true cache bypass path.
</objective>

<execution_context>
@/Users/ahmedmohamed/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/ahmedmohamed/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-snapshot-quotes-freshness/03-snapshot-quotes-freshness-VERIFICATION.md
@.planning/phases/03-snapshot-quotes-freshness/03-02-SUMMARY.md
@tradingagents/api/services/eodhd_client.py
@tradingagents/api/services/snapshots.py
@tradingagents/api/services/eodhd_cache.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Allow cache_ttl_seconds=0 to bypass EODHD cache</name>
  <files>tradingagents/api/services/eodhd_client.py</files>
  <action>
    Update EodhdClient.__init__ to treat a None cache_ttl_seconds as “use settings default,”
    but preserve explicit 0 so callers can disable caching. Replace any `or` fallback with
    an `is None` check (e.g., `settings.eodhd_cache_ttl_seconds if cache_ttl_seconds is None else cache_ttl_seconds`).
    Ensure get_quote uses the instance cache_ttl_seconds unchanged.
  </action>
  <verify>
    Inspect eodhd_client.py: __init__ uses an `is None` fallback and keeps 0 intact.
  </verify>
  <done>
    Passing cache_ttl_seconds=0 results in cache_ttl_seconds == 0 on the client instance.
  </done>
</task>

<task type="auto">
  <name>Task 2: Preserve fetched_at when building snapshot freshness</name>
  <files>tradingagents/api/services/snapshots.py</files>
  <action>
    In the snapshot service, ensure freshness labels are based on actual fetched_at values:
    - When serving cached data via load_cached_payload_with_meta, pass cache_meta.fetched_at into
      the freshness builder and response fields (do not overwrite with “now”).
    - When fetching from provider (EodhdClient with cache_ttl_seconds=0), capture a fetch_time
      (datetime.now(timezone.utc)) at the moment of provider retrieval and use that as fetched_at.
    - If provider fetch fails and stale cache is used, keep stale cache_meta.fetched_at in the response
      and label as stale (no fresh relabeling).
    Keep existing response shape and entitlement logic unchanged; only fix timestamps and labels.
  </action>
  <verify>
    Inspect snapshots.py: cached path uses cache_meta.fetched_at; provider path sets fetched_at from
    fetch_time; stale fallback keeps stale fetched_at.
  </verify>
  <done>
    Freshness labels reflect real cache/provider timestamps and cannot relabel cached data as fresh.
  </done>
</task>

</tasks>

<verification>
- Verify code review: cache bypass preserves 0 and freshness uses fetched_at sources correctly.
</verification>

<success_criteria>
- Provider fetch path truly bypasses cache when cache_ttl_seconds=0.
- Snapshot freshness labels reflect cache or provider fetched_at without masking stale data.
</success_criteria>

<output>
After completion, create `.planning/phases/03-snapshot-quotes-freshness/03-04-SUMMARY.md`
</output>
