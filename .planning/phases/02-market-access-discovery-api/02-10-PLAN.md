---
phase: 02-market-access-discovery-api
plan: "10"
type: execute
wave: 5
depends_on: ["02-04"]
files_modified:
  - tradingagents/api/services/market_registry.py
autonomous: true
gap_closure: true
user_setup:
  - service: eodhd
    why: "Provider-backed market registry data"
    env_vars:
      - name: EODHD_API_KEY
        source: "EODHD Dashboard -> API Keys"

must_haves:
  truths:
    - "Market listings use EODHD exchange details when an API key is configured."
    - "Market listings still include session status and next session timestamps even when provider data is cached or unavailable."
    - "Market registry falls back to cached exchange details when provider requests fail."
  artifacts:
    - path: "tradingagents/api/services/market_registry.py"
      provides: "Market registry builds provider-backed metadata merged with session schedules"
  key_links:
    - from: "tradingagents/api/services/market_registry.py"
      to: "tradingagents/api/services/eodhd_client.py"
      via: "EodhdClient.get_exchange_details"
      pattern: "get_exchange_details"
    - from: "tradingagents/api/services/market_registry.py"
      to: "tradingagents/api/services/eodhd_cache.py"
      via: "load_cached_payload"
      pattern: "load_cached_payload"
---

<objective>
Wire market registry to EODHD exchange details with cached fallback.

Purpose: Ensure /markets returns provider-backed metadata without breaking session schedule fields.
Output: Updated market registry that merges provider exchange details with static session defaults.
</objective>

<execution_context>
@/Users/ahmedmohamed/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/ahmedmohamed/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-market-access-discovery-api/02-market-access-discovery-api-VERIFICATION.md
@tradingagents/api/services/market_registry.py
@tradingagents/api/services/eodhd_client.py
@tradingagents/api/services/eodhd_cache.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge EODHD exchange details into market registry with cached fallback</name>
  <files>tradingagents/api/services/market_registry.py</files>
  <action>
    - Import EodhdClient and load_cached_payload to support provider fetch + cache fallback.
    - Add a helper to fetch exchange details for a market (by exchange code) that:
      1) calls EodhdClient.get_exchange_details,
      2) on failure, loads cached exchange details with a long TTL (e.g., 10 years),
      3) if cache missing, falls back to static defaults to keep /markets usable without a key.
    - Add a small extraction helper (case-insensitive) to read provider fields like Name, MIC, Currency, Timezone when present.
    - Update _build_market to merge provider metadata (name/mic/currency/timezone) with _MARKET_METADATA_DEFAULTS and _MARKET_SCHEDULES.
      Keep session_open/session_close/trading_days/data_delay_minutes/realtime_available driven by static defaults.
    - Keep response shape aligned with MarketResponse (do NOT add new fields).
    - Ensure list_markets/get_market still return session status + next timestamps via _serialize_market.
  </action>
  <verify>
    - Without EODHD_API_KEY: `python - <<'PY'\nfrom tradingagents.api.services import market_registry\nmarkets = market_registry.list_markets()\nprint(markets)\nassert all("status" in m and "next_open" in m and "next_close" in m for m in markets)\nPY`
    - With EODHD_API_KEY configured: `python - <<'PY'\nfrom tradingagents.api.services import market_registry\nmarkets = market_registry.list_markets()\nprint(markets)\nPY` and confirm provider metadata (name/mic/currency/timezone) reflects exchange details.
  </verify>
  <done>
    - Market registry calls EodhdClient.get_exchange_details when API key is present.
    - Cached exchange details are used when provider fetch fails.
    - Session status + next_open/next_close remain present on all market responses.
  </done>
</task>

</tasks>

<verification>
- With EODHD_API_KEY configured, /api/v1/markets returns provider-backed metadata and session status fields.
- With no API key, /api/v1/markets still responds using defaults without raising.
</verification>

<success_criteria>
- Market registry is provider-backed with cached fallback and preserves session schedule metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/02-market-access-discovery-api/02-10-SUMMARY.md`
</output>
