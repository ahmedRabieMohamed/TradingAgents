---
phase: 02-market-access-discovery-api
plan: "11"
type: execute
wave: 5
depends_on: ["02-04"]
files_modified:
  - tradingagents/api/services/symbols.py
autonomous: true
gap_closure: true
user_setup:
  - service: eodhd
    why: "Provider-backed symbol metrics and EOD series fallback"
    env_vars:
      - name: EODHD_API_KEY
        source: "EODHD Dashboard -> API Keys"

must_haves:
  truths:
    - "Symbol discovery returns metrics that fall back to EOD series when provider fields are missing."
    - "Discovery lists continue to return list metadata while using cached provider data when delayed."
  artifacts:
    - path: "tradingagents/api/services/symbols.py"
      provides: "Symbol discovery with cached metrics fallback"
  key_links:
    - from: "tradingagents/api/services/symbols.py"
      to: "tradingagents/api/services/eodhd_cache.py"
      via: "load_cached_payload"
      pattern: "load_cached_payload"
    - from: "tradingagents/api/services/symbols.py"
      to: "tradingagents/api/services/eodhd_client.py"
      via: "get_eod_series"
      pattern: "get_eod_series"
---

<objective>
Enable cached EOD series fallback for missing symbol metrics.

Purpose: Ensure discovery lists remain provider-backed when exchange symbol metrics are incomplete.
Output: Updated symbol metrics logic that can compute metrics from cached EOD series when needed.
</objective>

<execution_context>
@/Users/ahmedmohamed/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/ahmedmohamed/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-market-access-discovery-api/02-market-access-discovery-api-VERIFICATION.md
@tradingagents/api/services/symbols.py
@tradingagents/api/services/eodhd_cache.py
@tradingagents/api/services/eodhd_client.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable cached EOD series fallback for missing metrics</name>
  <files>tradingagents/api/services/symbols.py</files>
  <action>
    - Add a small helper to load cached EOD series using the same cache key scheme as EodhdClient (eod_{exchange}_{symbol}_{from}_{to}).
    - Update _get_metrics so that when avg_volume or change_pct is missing and allow_series is True:
      - First check cached series and compute avg_volume_1w + price_change_pct_1w from cached data.
      - Avoid per-symbol network calls on list endpoints; only use cached series data when available.
      - Keep the existing safe defaults (0.0) when neither provider fields nor cached series are available.
    - Update load_symbol_data to pass allow_series=True so the fallback path is enabled when provider metrics are missing.
  </action>
  <verify>
    - `python - <<'PY'\nfrom tradingagents.api.services import symbols\nitems = symbols.load_symbol_data("US")\nprint(items[:2])\nassert all("avg_volume_1w" in item and "price_change_pct_1w" in item for item in items[:2])\nPY`
    - With cached EOD series present, verify that missing provider metrics are populated from cached series (inspect a symbol with missing provider fields).
  </verify>
  <done>
    - load_symbol_data enables series fallback logic for missing provider metrics.
    - Cached EOD series data is used when available; list endpoints remain cache-first without per-symbol network fetches.
  </done>
</task>

</tasks>

<verification>
- /api/v1/symbols/* endpoints still return list metadata, and symbols include metrics even when provider fields are missing (using cached series when available).
</verification>

<success_criteria>
- Symbol discovery metrics use cached EOD series fallback without triggering per-symbol provider calls.
</success_criteria>

<output>
After completion, create `.planning/phases/02-market-access-discovery-api/02-11-SUMMARY.md`
</output>
