---
phase: 02-market-access-discovery-api
plan: 04
type: execute
wave: 4
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - tradingagents/api/services/eodhd_cache.py
  - tradingagents/api/services/eodhd_client.py
  - tradingagents/api/services/symbols.py
  - tradingagents/api/schemas/symbols.py
  - tradingagents/api/routers/symbols.py
  - tradingagents/api/routers/__init__.py
autonomous: true
user_setup:
  - service: eodhd
    why: "Market/symbol discovery source"
    env_vars:
      - name: EODHD_API_KEY
        source: "EODHD dashboard API key"

must_haves:
  truths:
    - "User can search symbols by ticker or company name within a market"
    - "User can filter symbols by sector and market cap, and request most active/trending"
    - "Most active is defined by volume, trending by 1-week price change"
  artifacts:
    - path: "tradingagents/api/services/eodhd_client.py"
      provides: "EODHD exchange symbol list + EOD series fetch"
    - path: "tradingagents/api/services/eodhd_cache.py"
      provides: "File cache with TTL for EODHD payloads"
    - path: "tradingagents/api/services/symbols.py"
      provides: "Search/filter/most-active/trending logic"
    - path: "tradingagents/api/schemas/symbols.py"
      provides: "SymbolResponse and SymbolListResponse"
    - path: "tradingagents/api/routers/symbols.py"
      provides: "/symbols discovery endpoints"
  key_links:
    - from: "tradingagents/api/routers/symbols.py"
      to: "tradingagents/api/services/symbols.py"
      via: "symbols_service.*"
      pattern: "symbols_service\."
    - from: "tradingagents/api/services/symbols.py"
      to: "tradingagents/api/services/eodhd_client.py"
      via: "EodhdClient.get_exchange_symbols"
      pattern: "get_exchange_symbols"
    - from: "tradingagents/api/routers/__init__.py"
      to: "tradingagents/api/routers/symbols.py"
      via: "include_router"
      pattern: "symbols_router"
    - from: "tradingagents/api/routers/__init__.py"
      to: "tradingagents/api/routers/auth.py"
      via: "include_router"
      pattern: "auth_router"
---

<objective>
Implement symbol discovery with search, filters, and top/trending lists backed by EODHD.

Purpose: Satisfy MKT-02/03/04 with stable discovery endpoints and Option A provider-backed symbol lists.
Output: EODHD client/cache, symbol services, schemas, endpoints, and router wiring.
</objective>

<execution_context>
@/Users/ahmedmohamed/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/ahmedmohamed/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-market-access-discovery-api/02-CONTEXT.md
@.planning/phases/02-market-access-discovery-api/02-RESEARCH.md
@tradingagents/api/services/market_registry.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add EODHD cache + client wrappers</name>
  <files>
tradingagents/api/services/eodhd_cache.py
tradingagents/api/services/eodhd_client.py
  </files>
  <action>
Implement file-based cache helpers with fetched_at timestamps and TTL checks.
Create EodhdClient with methods:
- get_exchange_symbols(exchange_code)
- get_eod_series(symbol, exchange_code, start_date, end_date)
Use settings.eodhd_api_key/base_url/cache_ttl_seconds and raise clear errors when API key missing and no cache exists.
Cache all responses via eodhd_cache helpers.
  </action>
  <verify>python -m compileall tradingagents/api/services/eodhd_cache.py tradingagents/api/services/eodhd_client.py</verify>
  <done>EODHD client fetches symbols/series and uses disk cache with TTL.</done>
</task>

<task type="auto">
  <name>Task 2: Implement symbol discovery service</name>
  <files>tradingagents/api/services/symbols.py</files>
  <action>
Implement discovery helpers backed by EODHD exchange-symbol list:
- load_symbol_data(market_id) maps provider fields to ticker/name/sector/market_cap/currency
- compute metrics avg_volume_1w and price_change_pct_1w from payload (fallback to 1-week EOD series when missing)
- search_symbols: prefix + contains match for ticker/name, sort by match quality then avg_volume_1w
- filter_symbols: sector + market_cap_min/max, sort by most_active or ticker
- get_most_active: order by avg_volume_1w with meta {definition: volume, window: 1w}
- get_trending: order by price_change_pct_1w with meta {definition: price_change_pct, window: 1w}
- paginate with limit/offset and next_offset
Validate market_id using market_registry and include currency from market metadata.
  </action>
  <verify>python -m compileall tradingagents/api/services/symbols.py</verify>
  <done>Symbol services support search, filters, most-active, trending with correct definitions and pagination.</done>
</task>

<task type="auto">
  <name>Task 3: Add symbol schemas, endpoints, and wire routers</name>
  <files>
tradingagents/api/schemas/symbols.py
tradingagents/api/routers/symbols.py
tradingagents/api/routers/__init__.py
  </files>
  <action>
Define SymbolResponse, SymbolListResponse, and ListMeta schemas.
Implement endpoints:
- GET /symbols/search (query or q required, market_id required)
- GET /symbols (filters: sector, market_cap_min/max, sort)
- GET /symbols/most-active (volume definition, window=1w default)
- GET /symbols/trending (price_change_pct definition, window=1w default)
Use optional_current_account and rate_limit.enforce_rate_limits. Return ApiError VALIDATION_ERROR when search query missing.
Update routers/__init__.py to include auth, markets, and symbols routers in api_router.
  </action>
  <verify>python -m compileall tradingagents/api/schemas/symbols.py tradingagents/api/routers/symbols.py tradingagents/api/routers/__init__.py</verify>
  <done>Symbol endpoints return paginated results and are wired into the API router.</done>
</task>

</tasks>

<verification>
- python -m compileall tradingagents/api
</verification>

<success_criteria>
- /symbols/search supports prefix+contains matching within a market.
- /symbols supports sector/market-cap filters and most_active sorting.
- /symbols/most-active and /symbols/trending return meta definitions and 1w window.
</success_criteria>

<output>
After completion, create `.planning/phases/02-market-access-discovery-api/02-04-SUMMARY.md`
</output>
