---
phase: 02-market-access-discovery-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tradingagents/api/settings.py
  - tradingagents/api/schemas/errors.py
  - tradingagents/api/deps/errors.py
  - tradingagents/api/deps/rate_limit.py
  - tradingagents/api/routers/__init__.py
  - tradingagents/api/app.py
  - tradingagents/api/__init__.py
autonomous: true
user_setup:
  - service: redis
    why: "Rate limiting"
    env_vars:
      - name: REDIS_URL
        source: "Redis instance connection string"

must_haves:
  truths:
    - "API responses return a standard error model with error.code and error.message"
    - "Rate-limit breaches return HTTP 429 with RATE_LIMIT_EXCEEDED"
    - "API app mounts versioned routes under /api/v1"
  artifacts:
    - path: "tradingagents/api/app.py"
      provides: "FastAPI app with exception handlers and router mounting"
    - path: "tradingagents/api/deps/errors.py"
      provides: "ApiError class and JSON error handlers"
    - path: "tradingagents/api/schemas/errors.py"
      provides: "ErrorResponse and ErrorDetail models"
    - path: "tradingagents/api/deps/rate_limit.py"
      provides: "Rate limit dependencies + enforcement helper"
    - path: "tradingagents/api/settings.py"
      provides: "Environment-driven API settings"
  key_links:
    - from: "tradingagents/api/app.py"
      to: "tradingagents/api/routers/__init__.py"
      via: "app.include_router(api_router, prefix=settings.api_prefix)"
      pattern: "include_router\(api_router"
    - from: "tradingagents/api/app.py"
      to: "tradingagents/api/deps/errors.py"
      via: "app.add_exception_handler"
      pattern: "add_exception_handler"
    - from: "tradingagents/api/deps/rate_limit.py"
      to: "fastapi_limiter.FastAPILimiter"
      via: "init on demand"
      pattern: "FastAPILimiter\.init"
---

<objective>
Establish the API foundation with settings, standard error model, and rate-limit helpers.

Purpose: Provide stable configuration, error handling, and throttling primitives used by all discovery endpoints.
Output: Settings module, error schemas/handlers, rate-limit dependencies, and FastAPI app shell.
</objective>

<execution_context>
@/Users/ahmedmohamed/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/ahmedmohamed/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-market-access-discovery-api/02-CONTEXT.md
@.planning/phases/02-market-access-discovery-api/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define API settings with env defaults</name>
  <files>tradingagents/api/settings.py</files>
  <action>
Create a Settings class using pydantic-settings (fallback to pydantic BaseSettings) with env-driven defaults:
- app_env (default dev)
- api_prefix (/api/v1), api_title/version
- jwt_secret/jwt_algorithm + access_token_ttl_minutes (default 1440)
- api_clients_json, dev_client_id/dev_client_secret
- redis_url
- eodhd_api_key/base_url/cache_dir/cache_ttl_seconds
- rate-limit quotas (auth/anon per minute/day)
Keep dev defaults in settings; guard production secrets in auth services instead of here.
Export settings = Settings().
  </action>
  <verify>python -m compileall tradingagents/api/settings.py</verify>
  <done>Settings exposes app_env plus required auth, rate-limit, and discovery configuration with dev defaults.</done>
</task>

<task type="auto">
  <name>Task 2: Implement standard error schemas and handlers</name>
  <files>
tradingagents/api/schemas/errors.py
tradingagents/api/deps/errors.py
  </files>
  <action>
Create ErrorDetail/ErrorResponse Pydantic models and ApiError exception type.
Implement JSON error handlers for:
- ApiError (code/message/details)
- HTTPException (map 401/403/404/429 to explicit codes)
- RequestValidationError (code VALIDATION_ERROR)
- RateLimitExceeded (code RATE_LIMIT_EXCEEDED) if fastapi-limiter is installed
Include optional request_id from x-request-id header. Use model_dump when available, fallback to dict().
  </action>
  <verify>python -m compileall tradingagents/api/schemas/errors.py tradingagents/api/deps/errors.py</verify>
  <done>Error responses consistently return {error: {code, message, details}, request_id} across handlers.</done>
</task>

<task type="auto">
  <name>Task 3: Add rate-limit helpers and FastAPI app shell</name>
  <files>
tradingagents/api/deps/rate_limit.py
tradingagents/api/routers/__init__.py
tradingagents/api/app.py
tradingagents/api/__init__.py
  </files>
  <action>
Implement rate_limit dependency helpers:
- limiter_identifier uses request.state.limiter_id, falling back to client IP
- RateLimiter instances for auth/anon quotas
- enforce_rate_limits(request, is_authenticated) with lazy FastAPILimiter init (and Redis URL guard)

Create routers/__init__.py with api_router = APIRouter() only (no domain routers yet).
Create app.py with FastAPI app, lifespan Redis init, exception handlers from deps/errors, and include_router(api_router, prefix=settings.api_prefix).
Export app in tradingagents/api/__init__.py.
  </action>
  <verify>python -m compileall tradingagents/api/deps/rate_limit.py tradingagents/api/app.py</verify>
  <done>App initializes with error handlers and rate-limit helpers, and mounts a versioned api_router.</done>
</task>

</tasks>

<verification>
- python -m compileall tradingagents/api
</verification>

<success_criteria>
- Standard error responses emit error.code/error.message with optional request_id.
- Rate limit enforcement helper exists and can initialize FastAPILimiter lazily.
- FastAPI app mounts /api/v1 and compiles without import errors.
</success_criteria>

<output>
After completion, create `.planning/phases/02-market-access-discovery-api/02-01-SUMMARY.md`
</output>
