---
phase: 02-market-access-discovery-api
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - tradingagents/api/schemas/auth.py
  - tradingagents/api/services/auth_tokens.py
  - tradingagents/api/services/client_registry.py
  - tradingagents/api/services/refresh_store.py
  - tradingagents/api/deps/auth.py
  - tradingagents/api/routers/auth.py
autonomous: true

must_haves:
  truths:
    - "Client credentials can exchange for access + refresh tokens"
    - "Refresh tokens rotate and invalidate older tokens"
    - "Authenticated requests are rate-limited using the authenticated bucket (not the anonymous limit)"
  artifacts:
    - path: "tradingagents/api/schemas/auth.py"
      provides: "TokenRequest, RefreshRequest, TokenResponse"
    - path: "tradingagents/api/services/auth_tokens.py"
      provides: "JWT encode/decode helpers with TTLs"
    - path: "tradingagents/api/services/refresh_store.py"
      provides: "Redis-backed refresh rotation"
    - path: "tradingagents/api/deps/auth.py"
      provides: "get_current_account and optional_current_account"
    - path: "tradingagents/api/routers/auth.py"
      provides: "/auth/token and /auth/refresh endpoints"
  key_links:
    - from: "tradingagents/api/routers/auth.py"
      to: "tradingagents/api/services/auth_tokens.py"
      via: "create_access_token/create_refresh_token"
      pattern: "create_access_token"
    - from: "tradingagents/api/routers/auth.py"
      to: "tradingagents/api/services/refresh_store.py"
      via: "store_refresh_token/verify_and_rotate"
      pattern: "verify_and_rotate"
    - from: "tradingagents/api/deps/auth.py"
      to: "tradingagents/api/services/auth_tokens.py"
      via: "decode_token"
      pattern: "decode_token"
---

<objective>
Implement JWT auth, client registry, refresh rotation, and auth endpoints.

Purpose: Provide API-01 compliant token issuance/refresh and authenticated identity wiring.
Output: Auth schemas, token helpers, client registry, refresh store, auth dependencies, and auth router.
</objective>

<execution_context>
@/Users/ahmedmohamed/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/ahmedmohamed/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-market-access-discovery-api/02-CONTEXT.md
@.planning/phases/02-market-access-discovery-api/02-RESEARCH.md
@tradingagents/api/settings.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build auth schemas, JWT helpers, and client registry</name>
  <files>
tradingagents/api/schemas/auth.py
tradingagents/api/services/auth_tokens.py
tradingagents/api/services/client_registry.py
  </files>
  <action>
Define TokenRequest, RefreshRequest, TokenResponse models.
Implement auth_tokens helpers:
- create_access_token and create_refresh_token with exp TTLs
- decode_token with PyJWT, raising ApiError AUTH_INVALID on invalid/expired
- guard production by rejecting dev-jwt-secret when app_env != dev

Implement client_registry:
- parse API_CLIENTS_JSON as a list of {client_id, client_secret_hash, account_id, name}
- raise ApiError AUTH_CONFIG_ERROR on invalid JSON/shape
- fall back to a single dev client (dev_client_id/secret) when app_env == dev
- verify_client_secret using pwdlib Argon2 hasher
  </action>
  <verify>python -m compileall tradingagents/api/schemas/auth.py tradingagents/api/services/auth_tokens.py tradingagents/api/services/client_registry.py</verify>
  <done>Auth models and JWT helpers compile, and client registry supports JSON config plus dev fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Redis-backed refresh token rotation</name>
  <files>tradingagents/api/services/refresh_store.py</files>
  <action>
Create refresh token store that:
- hashes refresh tokens with REFRESH_TOKEN_SALT (dev default only in dev)
- stores token hash by jti in Redis with TTL matching refresh_token_ttl_days
- verifies and rotates refresh tokens, deleting old jti and issuing new tokens
- raises ApiError REDIS_NOT_CONFIGURED when redis_url missing
- raises ApiError AUTH_INVALID for missing/invalid jti or hash mismatch
Use auth_tokens helpers for decode/create, and pwdlib Argon2 for hashing.
  </action>
  <verify>python -m compileall tradingagents/api/services/refresh_store.py</verify>
  <done>Refresh tokens are stored, verified, invalidated, and rotated with Redis backing.</done>
</task>

<task type="auto">
  <name>Task 3: Add auth dependencies and token endpoints</name>
  <files>
tradingagents/api/deps/auth.py
tradingagents/api/routers/auth.py
  </files>
  <action>
Implement get_current_account and optional_current_account using HTTPBearer:
- require Bearer scheme, decode token, populate request.state account_id/client_id/limiter_id
- return {account_id, client_id} or None for optional auth
- raise ApiError AUTH_INVALID for bad scheme/claims

Implement /auth/token and /auth/refresh endpoints:
- /auth/token validates client credentials, issues access+refresh, stores refresh
- /auth/refresh verifies and rotates refresh tokens
- apply anon rate-limit dependencies for both endpoints
- return TokenResponse with expires_in and refresh_expires_in seconds
  </action>
  <verify>python -m compileall tradingagents/api/deps/auth.py tradingagents/api/routers/auth.py</verify>
  <done>Auth endpoints issue/refresh tokens and populate request state for rate limiting.</done>
</task>

</tasks>

<verification>
- python -m compileall tradingagents/api
</verification>

<success_criteria>
- Valid client credentials return access + refresh tokens with TTLs.
- Refresh tokens rotate and invalidate prior jti entries.
- Bearer auth sets request.state.account_id for downstream rate limits.
</success_criteria>

<output>
After completion, create `.planning/phases/02-market-access-discovery-api/02-02-SUMMARY.md`
</output>
