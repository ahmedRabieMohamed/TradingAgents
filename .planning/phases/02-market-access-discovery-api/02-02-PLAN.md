---
phase: 02-market-access-discovery-api
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - tradingagents/api/schemas/auth.py
  - tradingagents/api/services/auth_tokens.py
  - tradingagents/api/services/client_registry.py
  - tradingagents/api/deps/auth.py
  - tradingagents/api/routers/auth.py
autonomous: true

must_haves:
  truths:
    - "Auth router defines a token issuance endpoint"
    - "Access tokens are JWTs with a 24h TTL"
  artifacts:
    - path: "tradingagents/api/schemas/auth.py"
      provides: "TokenRequest, TokenResponse"
    - path: "tradingagents/api/services/auth_tokens.py"
      provides: "JWT encode/decode helpers with TTLs"
    - path: "tradingagents/api/deps/auth.py"
      provides: "get_current_account and optional_current_account"
    - path: "tradingagents/api/routers/auth.py"
      provides: "/auth/token endpoint"
  key_links:
    - from: "tradingagents/api/routers/auth.py"
      to: "tradingagents/api/services/auth_tokens.py"
      via: "create_access_token"
      pattern: "create_access_token"
    - from: "tradingagents/api/deps/auth.py"
      to: "tradingagents/api/services/auth_tokens.py"
      via: "decode_token"
      pattern: "decode_token"
---

<objective>
Implement JWT auth, client registry, and token issuance endpoint.

Purpose: Provide API-01 compliant token issuance and authenticated identity wiring.
Output: Auth schemas, token helpers, client registry, auth dependencies, and auth router.
</objective>

<execution_context>
@/Users/ahmedmohamed/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/ahmedmohamed/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-market-access-discovery-api/02-CONTEXT.md
@.planning/phases/02-market-access-discovery-api/02-RESEARCH.md
@tradingagents/api/settings.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build auth schemas, JWT helpers, and client registry</name>
  <files>
tradingagents/api/schemas/auth.py
tradingagents/api/services/auth_tokens.py
tradingagents/api/services/client_registry.py
  </files>
  <action>
Define TokenRequest and TokenResponse models.
Implement auth_tokens helpers:
- create_access_token with exp TTL using settings.access_token_ttl_minutes (default 1440)
- decode_token with PyJWT, raising ApiError AUTH_INVALID on invalid/expired
- guard production by rejecting dev-jwt-secret when app_env != dev

Implement client_registry:
- parse API_CLIENTS_JSON as a list of {client_id, client_secret_hash, account_id, name}
- raise ApiError AUTH_CONFIG_ERROR on invalid JSON/shape
- fall back to a single dev client (dev_client_id/secret) when app_env == dev
- verify_client_secret using pwdlib Argon2 hasher
  </action>
  <verify>python -m compileall tradingagents/api/schemas/auth.py tradingagents/api/services/auth_tokens.py tradingagents/api/services/client_registry.py</verify>
  <done>Auth models and JWT helpers compile, and client registry supports JSON config plus dev fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Add auth dependencies and token endpoint</name>
  <files>
tradingagents/api/deps/auth.py
tradingagents/api/routers/auth.py
  </files>
  <action>
Implement get_current_account and optional_current_account using HTTPBearer:
- require Bearer scheme, decode token, populate request.state account_id/client_id/limiter_id
- return {account_id, client_id} or None for optional auth
- raise ApiError AUTH_INVALID for bad scheme/claims

Implement /auth/token endpoint:
- validates client credentials, issues access token only
- apply anon rate-limit dependency
- return TokenResponse with expires_in seconds
  </action>
  <verify>python -m compileall tradingagents/api/deps/auth.py tradingagents/api/routers/auth.py</verify>
  <done>Auth endpoint issues access tokens and populates request state for rate limiting.</done>
</task>

</tasks>

<verification>
- python -m compileall tradingagents/api
</verification>

<success_criteria>
- Auth router defines /auth/token handler returning TokenResponse with TTL.
- Access tokens are issued with a 24h expiry (settings.access_token_ttl_minutes = 1440).
- Bearer auth sets request.state.account_id for downstream rate limits.
</success_criteria>

<output>
After completion, create `.planning/phases/02-market-access-discovery-api/02-02-SUMMARY.md`
</output>
