---
phase: 02-market-access-discovery-api
plan: "09"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - tradingagents/api/app.py
  - tradingagents/api/deps/rate_limit.py
autonomous: true
gap_closure: true
user_setup:
  - service: redis
    why: "Enable rate limiting in UAT/prod"
    env_vars:
      - name: REDIS_URL
        source: "Redis instance connection string"
    dashboard_config: []

must_haves:
  truths:
    - "API can start in dev without REDIS_URL configured."
    - "Rate limiting returns 429 with RATE_LIMIT_EXCEEDED when REDIS_URL is configured."
  artifacts:
    - path: "tradingagents/api/app.py"
      provides: "Lifespan initializes limiter only when Redis is configured"
    - path: "tradingagents/api/deps/rate_limit.py"
      provides: "Rate limit enforcement no-ops in dev when Redis missing"
  key_links:
    - from: "tradingagents/api/app.py"
      to: "tradingagents/api/deps/rate_limit.py"
      via: "FastAPILimiter.init(..., identifier=limiter_identifier)"
      pattern: "FastAPILimiter\.init\(.*limiter_identifier"
    - from: "tradingagents/api/deps/rate_limit.py"
      to: "settings.redis_url"
      via: "conditional limiter init/skip"
      pattern: "redis_url"
---

<objective>
Remove the hard Redis startup requirement so the API boots in dev without REDIS_URL, while keeping rate limiting enforced when Redis is configured.

Purpose: Unblock UAT by preventing startup failure when REDIS_URL is missing.
Output: Updated lifespan and rate-limit enforcement logic.
</objective>

<execution_context>
@/Users/ahmedmohamed/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/ahmedmohamed/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-market-access-discovery-api/02-market-access-discovery-api-UAT.md
@.planning/phases/02-market-access-discovery-api/02-01-SUMMARY.md
@.planning/phases/02-market-access-discovery-api/02-02-SUMMARY.md
@tradingagents/api/app.py
@tradingagents/api/deps/rate_limit.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make lifespan Redis init optional in dev</name>
  <files>tradingagents/api/app.py</files>
  <action>
Update the FastAPI lifespan so Redis/fastapi-limiter initialization is skipped when REDIS_URL is missing in dev.
- If settings.redis_url is empty AND settings.app_env == "dev": skip limiter init and continue startup.
- If settings.redis_url is empty AND settings.app_env != "dev": raise RuntimeError as before.
- If REDIS_URL is present: initialize FastAPILimiter as-is.
- Keep imports inside lifespan to avoid import-time failures.
  </action>
  <verify>APP_ENV=dev python - <<'PY'
from fastapi import FastAPI
from tradingagents.api.app import lifespan
import asyncio

async def main():
    async with lifespan(FastAPI()):
        pass

asyncio.run(main())
print("lifespan ok without REDIS_URL")
PY</verify>
  <done>Running lifespan in dev without REDIS_URL completes without raising.</done>
</task>

<task type="auto">
  <name>Task 2: No-op rate limiting when Redis is absent in dev</name>
  <files>tradingagents/api/deps/rate_limit.py</files>
  <action>
Adjust limiter enforcement to align with optional Redis startup:
- In _ensure_limiter_initialized, if settings.redis_url is missing and app_env == "dev", return without initializing.
- If settings.redis_url is missing and app_env != "dev", keep raising RuntimeError.
- Preserve existing behavior when REDIS_URL is set (initialize and enforce limits).
- Do not change limiter identifiers or rate-limit thresholds.
  </action>
  <verify>APP_ENV=dev python - <<'PY'
import asyncio
from tradingagents.api.deps import rate_limit

async def main():
    await rate_limit._ensure_limiter_initialized()

asyncio.run(main())
print("rate limiter optional in dev")
PY</verify>
  <done>Rate limiter initialization is a no-op in dev without REDIS_URL, but still required in non-dev.</done>
</task>

</tasks>

<verification>
- Re-run UAT smoke: server starts without REDIS_URL and /api/v1/auth/token responds.
- With REDIS_URL set, rate-limit test returns 429 with RATE_LIMIT_EXCEEDED.
</verification>

<success_criteria>
- API boot does not fail when REDIS_URL is absent in dev.
- Rate limiting is enforced when Redis is configured.
</success_criteria>

<output>
After completion, create `.planning/phases/02-market-access-discovery-api/02-09-SUMMARY.md`
</output>
