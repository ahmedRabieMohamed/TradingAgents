---
phase: 02-market-access-discovery-api
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - tradingagents/api/services/market_registry.py
  - tradingagents/api/schemas/markets.py
  - tradingagents/api/routers/markets.py
autonomous: true

must_haves:
  truths:
    - "User can list available markets (US, EGX) with session status"
    - "User can request a specific market by id and see next session times"
    - "Invalid market ids return MARKET_NOT_FOUND"
    - "Authenticated requests are rate-limited using the authenticated bucket (not the anonymous limit)"
    - "Rate-limit breaches return HTTP 429 with RATE_LIMIT_EXCEEDED"
  artifacts:
    - path: "tradingagents/api/services/market_registry.py"
      provides: "Static market metadata + session status computation"
    - path: "tradingagents/api/schemas/markets.py"
      provides: "MarketResponse and MarketListResponse"
    - path: "tradingagents/api/routers/markets.py"
      provides: "/markets and /markets/{market_id} endpoints"
  key_links:
    - from: "tradingagents/api/routers/markets.py"
      to: "tradingagents/api/services/market_registry.py"
      via: "list_markets/get_market"
      pattern: "market_registry\."
    - from: "tradingagents/api/routers/markets.py"
      to: "tradingagents/api/deps/rate_limit.py"
      via: "rate_limit.enforce_rate_limits"
      pattern: "enforce_rate_limits"
---

<objective>
Deliver market registry data and /markets endpoints with session status.

Purpose: Satisfy MKT-01 with stable market selection metadata, aligned to Option A static registry.
Output: Market registry service, response schemas, and endpoints for list + detail.
</objective>

<execution_context>
@/Users/ahmedmohamed/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/ahmedmohamed/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-market-access-discovery-api/02-CONTEXT.md
@.planning/phases/02-market-access-discovery-api/02-RESEARCH.md
@tradingagents/api/deps/auth.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build static market registry with session status</name>
  <files>tradingagents/api/services/market_registry.py</files>
  <action>
Implement a static market registry (Option A) for US and EGX:
- Metadata: name, mic, currency, timezone, session_open/close, trading_days
- Status: compute open/closed, next_open, next_close using ZoneInfo and local time
- Include data_delay_minutes + realtime_available in market metadata
Provide list_markets() and get_market(market_id) helpers. Do NOT call EODHD exchange-details for /markets.
  </action>
  <verify>python -m compileall tradingagents/api/services/market_registry.py</verify>
  <done>Market registry returns static metadata plus status/next session times for US and EGX.</done>
</task>

<task type="auto">
  <name>Task 2: Expose /markets endpoints with schemas and rate limits</name>
  <files>
tradingagents/api/schemas/markets.py
tradingagents/api/routers/markets.py
  </files>
  <action>
Create MarketResponse and MarketListResponse schemas including status, next_open, next_close.
Implement /markets and /markets/{market_id} endpoints:
- optional auth via optional_current_account
- enforce rate limits based on request.state.account_id
- return MARKET_NOT_FOUND ApiError for invalid market ids
  </action>
  <verify>python -m compileall tradingagents/api/schemas/markets.py tradingagents/api/routers/markets.py</verify>
  <done>Market endpoints return detailed payloads and enforce rate limits for anon/auth users.</done>
</task>

</tasks>

<verification>
- python -m compileall tradingagents/api
</verification>

<success_criteria>
- /markets returns US and EGX with status + next session timestamps.
- /markets/{market_id} returns 404 MARKET_NOT_FOUND for invalid ids.
- Requests respect anon vs auth rate limits.
</success_criteria>

<output>
After completion, create `.planning/phases/02-market-access-discovery-api/02-03-SUMMARY.md`
</output>
